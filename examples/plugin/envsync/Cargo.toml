[package]
name = "example-agent-envsync"
version = "0.1.0"
edition = "2021"
description = "Example scheduled-job WASM plugin: syncs a keyring secret into the host env"

[lib]
crate-type = ["cdylib"]

[dependencies]
openagent-pdk = { path = "../../../plugin/pdk/rust" }

[profile.release]
panic = "abort"
